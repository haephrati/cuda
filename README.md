# cuda

CUDA molecular-surface sample (`simple-gen-modecular`) with `CommonPara.h` chain limits and amino-acid tables.

## Unit tests

`Tests/CudaMolecularTests` checks `maxnumchains` is 1200 and `aad3[0]` is ALA. Build Release | x64 in Visual Studio.
